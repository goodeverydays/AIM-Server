# 复制为 .env 并填入真实值（.env 已被 gitignore，不会提交）
# QQ 邮箱：在 设置→账号→POP3/SMTP 开启服务并生成「授权码」（不是登录密码）

SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USERNAME=你的邮箱@qq.com
SMTP_PASSWORD=授权码
SMTP_FROM=你的邮箱@qq.com
SMTP_USE_TLS=true

# 可选：验证码策略
# MAIL_CODE_TTL_SECONDS=300
# MAIL_CODE_COOLDOWN_SECONDS=60
